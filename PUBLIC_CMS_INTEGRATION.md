# ربط الواجهة العامة بـ Google Sheets CMS

تعمل الصفحة العامة بالترتيب: fallback محلي آمن، ثم `mhr_cache_v2`، ثم تحديث خلفي من Web App Apps Script `/exec`. مدة صلاحية الكاش ثابتة أربع ساعات. بنية الكاش: `schemaVersion: "2.0"` و`fetchedAt` بصيغة ISO و`source` و`data` العامة فقط. يُحاول الموقع ترحيل `mhr_cache_v1` والمفتاح الانتقالي السابق من دون حذف أي منهما.

لا يُستخدم رابط `pubhtml` كـAPI. يُضبط `CONTENT_API_URL` داخل `script.js` على رابط Web App الإنتاجي الوحيد الذي ينتهي بـ`/exec` بعد التحقق من استجابته. لا يرسل الطلب Tokens أو Cookies أو بيانات مستخدم، وله timeout ثابت 10 ثوانٍ.

تتحكم Sheets بعد النشر في النصوص العامة، الأقسام، الإعلانات، الكروت، النماذج، الخدمات، السياسات، التنقل، روابط التواصل، والتدرج اللوني المحدود. HTML وCSS وJavaScript والمصادقة والـendpoint ومفاتيح التخزين وService Worker تبقى داخل الكود. القيم تمر بفحص دفاعي آخر في المتصفح؛ الروابط غير الآمنة تعطل الزر، والسياسات لا تقبل إلا HTTPS، ولا يُخزن `auditLog` أو `schema`.

تم التحقق من قارئ Apps Script المنشور للقراءة العامة فقط واختبار تعديل نص ترحيبي محدود ثم إعادته. للاختبار المحلي شغّل `python -m http.server 8000` ثم افتح الصفحة؛ عند فشل API يستمر آخر cache صالح أو fallback. الكتابة من لوحة الإدارة وPhase B ما زالت غير مفعلة.
